{% extends 'base.html.twig' %}

{% block content %}


{#
    <h1>Créer une nouvelle catégorie </h1>
<br/>
    {{ form_start(form) }}
        {{ form_widget(form.nom) }}
    {{ form_widget(form.icone)}}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('categorieaide_index') }}">Back to the list</a>
        </li>
    </ul>
#}


<div>
<div class="">
        <h3>Créer une nouvelle catégorie</h3>
</div>

<div class="row">
    <div class="x_panel">
        <div class="x_content">

    {{ form_start(form) }}



    <div class="item form-group">
        <label class="col-form-label col-md-3 col-sm-3 label-align" for="{{ form.nom.vars.id }}">Nom</label>
        <input class="col-md-6 col-sm-6 form-control"   type="text"   id="{{ form.nom.vars.id }}" name="{{ form.nom.vars.full_name }}" >
        {{ form_errors(form.nom) }}

        {% do form.nom.setRendered %}
    </div>



    <div class="item form-group">
        <label class="col-form-label col-md-3 col-sm-3 label-align" for="{{ form.icone.vars.id }}">Icone</label>

        <input class="col-md-6 col-sm-6 " type="file"    type="text"   id="{{ form.icone.vars.id }}" name="{{ form.icone.vars.full_name }}" >
        {{ form_errors(form.icone) }}

        {% do form.icone.setRendered %}
    </div>


            <div class="ln_solid"></div>


            <div class="item form-group">
                <div class="col-md-6 col-sm-6 offset-md-3">
                    <button class="btn btn-primary" type="reset"><i class="fa fa-refresh"> </i> Réinitialiser</button>
                    <input class="btn btn-success" type="submit" value="Créer" />

                </div>
            </div>

    <a href="{{ path('categorieaide_index') }}">Back to the list</a>




    {{ form_end(form) }}



        </div>
    </div>
</div>

</div>






{% endblock %}


