{% extends 'front.html.twig' %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('front/scss/aide/css/root.css') }}">
{% endblock %}

{% block body %}
    <h1>Nous analysons votre signalement le plus tôt possible</h1>
    <h2>merci pour votre collaboration</h2>

    <table>
        <tbody>

            <tr>
                <th>Raison</th>
                <td>{{ signalementAide.raison }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ signalementAide.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('signalementaide_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('signalementaide_edit', { 'id': signalementAide.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
