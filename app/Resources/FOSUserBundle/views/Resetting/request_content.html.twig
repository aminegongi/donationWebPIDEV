{% trans_default_domain 'FOSUserBundle' %}

<div class="login-slide slide login-style1">
    <h2>Mot de Passe oublié </h2>

    <form action="{{ path('fos_user_resetting_send_email') }}" method="POST" class="fos_user_resetting_request">
        <div class="form-group">
            <label for="username" class="label" ></label>
            <input type="text" class="form-control" id="username" name="username" required="required" />
        </div>
        <div class="form-group">
            <input type="submit" class="submit" value="{{ 'resetting.request.submit'|trans }}" />
        </div>
    </form>
    <div  class="sign-up-txt"  >
        Vous vous souvenez de votre mot de passe ? <a href="{{ path('fos_user_security_login') }}" class="login-click">login</a>
    </div>
</div>
